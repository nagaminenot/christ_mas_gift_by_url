<%= form_with(model: gift, local: true) do |form| %>
  <% if gift.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gift.errors.count, "error") %> prohibited this gift from being saved:</h2>

      <ul>
      <% gift.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :gift_name %>
    <%= form.text_field :gift_name %>
  </div>

  <div class="field">
    <%= form.label :giver_name %>
    <%= form.text_field :giver_name %>
  </div>

  <div class="field">
    <%= form.label :taker_name %>
    <%= form.text_field :taker_name %>
  </div>

  <div class="field">
    <%= form.label :gift_url %>
    <%= form.text_field :gift_url %>
  </div>

  <div class="field">
    <%= form.label :image_url %>
    <%= form.text_field :image_url %>
  </div>

  <div class="field">
    <%= form.label :message %>
    <%= form.text_field :message %>
  </div>

  <div class="field">
    <%= form.label :slug %>
    <%= form.text_field :slug %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
